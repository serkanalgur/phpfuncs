language: go
go:
  - 1.15.6
install:
  - export GOPATH=$HOME/go
  - go get -t -v ./...
os: linux
dist: xenial
script:
  - go test -coverprofile=coverage.txt -covermode=atomic
  - go test -v $GOPATH/src/github.com/serkanalgur/phpfuncs/tests/...
after_success:
  - bash <(curl -s https://codecov.io/bash)
